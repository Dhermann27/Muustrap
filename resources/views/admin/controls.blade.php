<span class="hidden-print">
    <a href="{{ url('/household/' . $id) }}" class="fa fa-home"></a>
    <a href="{{ url('/camper/' . $id) }}" class="fa fa-group"></a>
    <a href="{{ url('/payment/' . $id) }}" class="fa fa-money"></a>
    <a href="{{ url('/workshopchoice/' . $id) }}" class="fa fa-rocket"></a>
    <a href="{{ url('/roomselection/' . $id) }}" class="fa fa-bed"></a>
</span>